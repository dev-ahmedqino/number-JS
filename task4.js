var Value= prompt("Enter the word to change the character");

var lowerLetter = "abcdefghijklmnopqrstuvwxyz"
function caseChange() {
    for (var i=0 ; i<Value.length ; i++) { 
        if (lowerLetter.includes(word[i])) {
            Value.toUpperCase()
        } else (Value.toLowerCase)
    }
}
caseChange()