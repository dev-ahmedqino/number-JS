var firstValue= prompt("Enter First Number");

var secValue= prompt("Enter Second Number");

function biggest() { 
if (firstValue>secValue) {
        alert ("The biggest Value are " + firstValue)
    } else (alert("The biggest Value are " + secValue));
}
biggest()